RideHero v7.9 - Real Loop Manual Motion

This build fixes two requested items:
- The floating mode spinner widget is hidden until after the user makes a Quick/Plan mode choice.
- The intro coaster animation no longer relies on an approximate SVG path for the loop.
- The coaster now uses manual circular motion:
  - enters from the left
  - enters the loop at the bottom
  - travels a true 360-degree circular loop
  - exits smoothly to the right
- Visible track art was simplified to a real circular vertical loop with entry and exit track segments.

Preserved:
- Mode choice screen
- Swipe panel pull behavior
- Red Quick Route / navy Plan mode
- Floating spinner after choice
- Disney hub, maps, routing, and UI improvements

Deployment marker:
RideHero v7.9 2026-07-17
