RideHero v7.8 - Near-Imitation Loop Pass

This build is a focused intro-only polish pass:
- Rebuilt the visible loop again with a cleaner near-reference vertical loop shape.
- Uses separate back/front loop layers so the track no longer looks like it cuts itself in half.
- Uses a hidden motion path with tangent entry and exit.
- Adds smoother train rotation with angle normalization to prevent snapping.
- Adds custom coaster progress timing:
  - smooth entry
  - readable loop climb
  - smoother top arc
  - natural exit
- Preserves the storyboard page peel, hidden rope, full-screen mode choice, floating mode widget, Disney hub, routing fixes, and all v7.6 behavior.

Deployment marker:
RideHero v7.8 2026-07-17
