RideHero v6.0 - Exaggerated Loop

This build makes the intro loop much bigger and more dramatic:
- Replaces the center loop with a much larger exaggerated vertical loop.
- Slows the intro a bit more so the big loop reads clearly.
- Keeps the no-dialog intro, mid-loop grab, page pull, and ride-away finish.
- Preserves all homepage, routing, and UI improvements.

Deployment marker:
RideHero v6.0 2026-07-16
