RideHero v5.2 - Smooth Coaster Motion

This build fixes the intro so the coaster follows the track smoothly instead of jumping between keyframes.

Changes:
- Train follows the SVG track with requestAnimationFrame + path sampling
- Smooth rotation as it enters and exits the loop
- Derail only after finishing the track path
- Page pull starts after the ride finishes
- Keeps the franchise hub, Disney grouping, and prior routing/UI fixes

Deployment marker:
RideHero v5.2 2026-07-16
