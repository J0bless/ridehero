RideHero v8.1 - Continuous Track + Articulated Coaster Cars

This build polishes the opening coaster animation:
- Replaced separated entry/loop/exit visuals with a single continuous track path.
- The visible track now reads as one route instead of separate disconnected pieces.
- Reworked the train motion so each coaster car follows the same track with a small distance offset.
- Each car rotates to its own local tangent, so the train bends around the loop like a real coaster.
- Preserves the clean pull-away with no visible white peel/rope artifact.
- Preserves mode spinner hidden until mode choice, full-screen mode choice, red Quick Route, navy Plan mode, Disney hub, maps, routing, and UI improvements.

Deployment marker:
RideHero v8.1 2026-07-17
