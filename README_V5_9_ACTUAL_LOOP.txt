RideHero v5.9 - Actual Loop

This build fixes the intro track so it has a true vertical center loop:
- Replaces the hill-like center feature with a real loop.
- Slightly slows the intro so the loop reads more clearly.
- Keeps the no-dialog intro.
- Keeps the mid-loop grab and page pull behavior.
- Preserves all prior homepage/routing/UI improvements.

Deployment marker:
RideHero v5.9 2026-07-16
