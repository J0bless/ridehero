RideHero update: route-only dynamic Animal Kingdom route

Changed:
- Removed the full visual walkway network overlay.
- Only the optimized route is shown on the map.
- Route line still follows the detailed walking-path graph behind the scenes.
- Added animated dashed route line and small direction markers so the route feels less static.
- Added walking-route accuracy disclaimer.
- Location button now checks whether the user is inside Animal Kingdom; outside users stay in preview mode.

Upload to repo root:
- index.html
- animal_kingdom_app_map.png

Optional:
- animal_kingdom_app_map.jpg is included for convenience, but the app uses the PNG.