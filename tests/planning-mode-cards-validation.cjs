const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const navigation = fs.readFileSync(path.join(__dirname, '..', 'js', 'navigation.js'), 'utf8');
const css = fs.readFileSync(path.join(__dirname, '..', 'css', 'onboarding.css'), 'utf8');

assert.match(navigation, /class="mode-card-grid"/, 'planning modes must render together in the restored card grid');
assert.match(navigation, /mode-card-quick[\s\S]*mode-card-full/, 'Quick Route and Maximize My Day cards must both be present');
assert.match(navigation, /Rides only/, 'Quick Route must clearly remain rides only');
assert.match(navigation, /Pull right to select[\s\S]*Pull left to select/, 'each card must explain its selection pull direction');
assert.doesNotMatch(navigation, /mode-card-preview/, 'the old Nearby, Live, Priority, and Balanced detail blocks must remain removed');
assert.doesNotMatch(navigation, /mode-screen-divider|mode-screen-cart/, 'the squiggly divider and decorative cart must remain removed');
assert.doesNotMatch(navigation, /data-mode-swipe|modeTextOpacities|ResizeObserver/, 'the width-compressing carousel must remain removed');
assert.doesNotMatch(navigation, /rideheroModeQuestionShown|firstQuestionThisSession/, 'question timing must not vary between visits');
assert.match(navigation, /questionHoldDuration = 2000/, 'every planning question view must remain readable for two seconds');
assert.match(navigation, /setTimeout\(revealOptions, questionHoldDuration \+ 380\)/, 'every question view must retain the existing 380ms fade');
assert.match(navigation, /options\.inert = true[\s\S]*options\.inert = false/, 'hidden options must remain inaccessible until revealed');
assert.match(navigation, /pointerdown[\s\S]*pointermove[\s\S]*pointerup[\s\S]*pointercancel/, 'the planning cards must support a complete reversible pull gesture');
assert.match(navigation, /pullDirection = activeMode === 'quick' \? 1 : -1/, 'Quick must pull right and Maximize My Day must pull left');
assert.match(navigation, /activeCard\.style\.transform = 'translate3d\('/, 'the selected card itself must follow the pointer');
assert.match(navigation, /allowedDistance >= width \* 0\.28/, 'card selection must require an intentional pull threshold');
assert.match(navigation, /function returnCard\(\)[\s\S]*translate3d\(0,0,0\)/, 'incomplete pulls must return the card smoothly');
assert.match(navigation, /document\.body\.classList\.add\('mode-screen-active'\)[\s\S]*function initModePull/, 'the mode screen must mark the shell while it is active');
assert.match(navigation, /document\.body\.classList\.remove\('mode-screen-active'\)/, 'later pages must restore the footer shell');

assert.match(css, /\.mode-catalog-page\{[^}]*linear-gradient/, 'planning mode should use the reference-inspired soft backdrop');
assert.match(css, /\.mode-card-grid\{[^}]*repeat\(2,minmax\(0,1fr\)\)/, 'wide layouts must show two balanced mode cards');
assert.match(css, /@media \(max-width:700px\)[\s\S]*\.mode-card-grid\{grid-template-columns:repeat\(2,minmax\(0,1fr\)\)/, 'mobile layouts must keep both planning options visible');
assert.match(css, /\.mode-card\{[^}]*border-radius:34px[^}]*background:var\(--mode-surface\)/, 'mode cards must use clean rounded mode-colored surfaces');
assert.match(css, /\.mode-card\{[^}]*display:grid[^}]*grid-template-rows:auto minmax\(0,1fr\) auto auto/, 'both cards must share symmetrical content, cue, and action rows');
assert.match(css, /\.mode-card-quick\{--mode-accent:var\(--rh-sixers-red\);--mode-secondary:var\(--rh-retro-orange\)/, 'Quick Route must use updated red with orange and white support');
assert.match(css, /\.mode-card-full\{--mode-accent:var\(--rh-vintage-blue\);--mode-secondary:var\(--rh-retro-green\)/, 'Full Day must use updated blue with navy and green support');
assert.match(css, /\.mode-card-kicker\{[^}]*min-height:2\.4em/, 'mode kickers must reserve matching vertical space');
assert.match(css, /\.mode-card-copy strong\{[^}]*min-height:2\.04em/, 'mode titles must reserve matching two-line space');
assert.match(css, /\.mode-card-pull-cue\{[^}]*display:flex/, 'direction instructions must use a stable, readable box');
assert.doesNotMatch(css, /mode-swipe-stage|mode-swipe-track|mode-progress-rail|mode-progress-cart|mode-switch-actions/, 'swipe-specific visual UI must remain removed');
assert.doesNotMatch(css, /mode-screen-divider|mode-screen-cart|mode-card-preview/, 'obsolete divider, cart, and detail-block CSS must remain removed');
assert.doesNotMatch(css, /mode-card-copy[^}]*white-space:\s*nowrap|mode-card-copy[^}]*scaleX/, 'mode copy must use normal unsquashed wrapping');
assert.match(navigation, /mode-choice-quick'[\s\S]*mode-choice-full'/, 'selection must set a directional page-swipe class');
assert.match(css, /body\.mode-choice-made \.mode-card-top[\s\S]*opacity:0/, 'mode text and content must fade during selection');
assert.match(css, /\.mode-choice-stage\{[^}]*flex:1[^}]*min-height:clamp\(420px,52dvh,620px\)/, 'planning options must fill the usable screen');
assert.match(css, /body\.mode-screen-active \.app-nav\{display:none!important\}/, 'the footer menu must stay hidden on the planning mode screen');
assert.match(css, /\.mode-catalog-page\.is-burning \.catalog-heading\{[^}]*opacity:0[^}]*filter:blur\(6px\)/, 'the opening question must burn and fade away without changing width');
assert.match(css, /\.mode-catalog-page\.is-options-ready \.catalog-content\{opacity:1;visibility:visible/, 'the options page must appear only after the opening effect');
assert.match(css, /mode-choice-quick \.mode-catalog-page\{opacity:0;transform:translate3d\(100vw,0,0\)\}/, 'Quick selection must finish in its rightward pull direction');
assert.match(css, /mode-choice-full \.mode-catalog-page\{opacity:0;transform:translate3d\(-100vw,0,0\)\}/, 'Full selection must finish in its leftward pull direction');
assert.doesNotMatch(css, /mode-choice-made[^}]*width|mode-choice-made[^}]*scaleX/, 'the effect must not alter page or text width');

console.log('Draggable planning mode card UI validation passed.');
